# QuickRec Lite v0.1 原型说明

## 范围

本原型验证 v0.1 新增或改变的用户可见状态：首次配置导入、使用默认设置、取消启动、迁移保存失败、设置保存失败、快捷键冲突、重复实例和 Lite 独立身份反馈。录制主流程沿用 Lite v0，不在原型中扩展新录制能力。

## 打开方式

```powershell
python -m http.server 8775 --directory E:\codex\QuickRec-Lite\doc\releases\lite-v0.1\prototype
```

浏览器访问：`http://127.0.0.1:8775/index.html`

## 交互清单

- 顶部场景切换：迁移、设置、重复实例、主界面。
- “导入设置”：模拟校验旧配置并原子写入成功。
- “使用 Lite 默认设置”：模拟新建默认配置。
- 迁移页关闭：模拟取消启动并保持零写入。
- “模拟保存失败”：进入可重试失败状态。
- 设置页“保存设置”：成功时展示持久化反馈。
- 设置页“模拟快捷键冲突”：保留旧值并展示冲突反馈。
- 设置页“模拟配置写入失败”：保持页面和原有效配置。
- 重复实例页“退出本次启动”：模拟第二实例退出。

## 模拟边界

HTML 不操作真实 APPDATA、注册表、互斥锁、快捷键或文件系统。所有路径、文件哈希和成功/失败状态都是用于确认需求合同的演示数据，不能作为实现或验收证据。

## 浏览器验证记录

- 状态：通过
- 验证日期：2026-08-11
- 桌面视口：`1440 x 900`，页面宽度与视口一致，主窗口、行为合同和操作区无横向溢出
- 窄视口：`600 x 900`，页面采用单列布局，正文宽度 `585px`，无横向溢出
- 关键交互：场景切换、迁移保存失败、使用默认设置、快捷键冲突、配置写入失败和重复实例均按合同切换
- 控制台错误：0
- 模拟边界：未访问真实 APPDATA、注册表、互斥锁、快捷键或录制链路
- 结论：原型门禁通过，可以进入 PyQt 实现
