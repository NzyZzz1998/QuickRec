# QuickRec Full v1.9.3 原型自动验证记录

## 1. 验证对象

| 项 | 内容 |
| --- | --- |
| 验证日期 | 2026-07-28 |
| 原型入口 | `doc/releases/v1.9.3/prototype/index.html?page=projects` |
| 本地地址 | `http://127.0.0.1:8772/index.html?page=projects` |
| 验证范围 | 基础剪辑、全局波纹、轨道锁定、片段检查器、响应式布局 |
| 产品确认 | 已完成（2026-07-29） |

本记录只证明原型的静态合同、浏览器交互与布局检查通过，不代表 PyQt
业务实现、播放准确性技术门禁或候选发布包验收通过。

## 2. 静态检查

| 检查项 | 结果 |
| --- | --- |
| `tests/test_v193_prototype.py` | 10 项通过 |
| `app.js` JavaScript 语法 | 通过 |
| `timeline.js` JavaScript 语法 | 通过 |
| UTF-8 与乱码扫描 | 通过 |
| `git diff --check` | 通过 |

## 3. 浏览器布局检查

| 视口 | 结果 | 重点结论 |
| --- | --- | --- |
| 1920×1080 | 通过 | 默认最大化布局完整，时间线使用全部可用宽度 |
| 1216×760 | 通过 | 按钮无裁切，工具栏无碰撞，页面无横向溢出 |
| 960×640 | 通过 | 核心操作可见，检查器与冲突弹窗可完整关闭 |

1216×760 的自动几何检查结果：

- 文本裁切按钮：0；
- 工具栏控件碰撞：0；
- 页面横向溢出：0；
- 浏览器运行时错误：0。

## 4. 交互检查

| 交互 | 结果 |
| --- | --- |
| 打开、校验、应用和取消片段属性 | 通过 |
| 鼠标拖动裁剪手柄并取消 | 通过，正式时间线零副作用 |
| 裁剪关联视频与音频 | 通过，来源范围保持一致 |
| 按播放头分割关联音视频 | 通过，右侧片段获得新身份并被选中 |
| 全局波纹影响预览 | 通过 |
| 锁定轨道冲突阻断 | 通过，确认按钮禁用且可定位冲突 |
| 全局波纹删除 | 通过，受影响片段按规则左移 |
| 保存失败回滚状态 | 通过 |
| 开发标注与控件合同注册 | 通过，共注册 189 个控件合同 |

## 5. 外部证据

浏览器截图保存在：

`E:\QRtest\QuickRec-v1.9.3-prototype`

主要证据：

- `default-fit-1920x1080.png`
- `clip-inspector-fullheight.png`
- `ripple-preview-1920x1080.png`
- `split-result-1920x1080.png`
- `ripple-lock-conflict.png`
- `default-1216x760.png`
- `default-960x640.png`
- `inspector-960x640.png`
- `ripple-conflict-960x640.png`

证据目录不属于产品运行依赖，也不纳入发布包。

## 6. 产品确认与剩余门禁

产品负责人已实际打开原型，并确认：

- 剪辑工具栏、双侧裁剪手柄和片段检查器的信息层级；
- 全局波纹预览、锁定冲突及取消行为；
- 最小窗口下的操作密度。

剩余门禁：

- 完成并确认 `dev_plan.md` 与 `progress.md`；
- 单独取得进入业务实现的明确授权；
- 正式剪辑领域和 PyQt 手势实现前完成非零源入点播放技术 spike。
