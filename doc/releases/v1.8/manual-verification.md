# QuickRec Full v1.8 GUI 手动验收清单

## 1. 验收对象

- 产品：QuickRec Full v1.8 工作台候选包。
- 分支：`test`。
- HEAD：`8fdf902ac5216bedd6efcd932b83e78b7d14b2c4`，工作区包含尚未提交的 v1.8 实现。
- 历史工作台候选 EXE：`E:\QRtest\QuickRec-v1.8-workbench-candidate-20260724-r5\QuickRec\QuickRec.exe`。
- 历史 EXE SHA256：`8735DAB9AF7A3631636E8CFCFCE77A82EA1C1C37112B059B857ABEF838C7B407`。
- 历史工作台隔离目录：`E:\QRtest\QuickRec-v1.8-workbench-acceptance-20260724-r5`。
- 录制链路证据目录：`E:\QRtest\QuickRec-v1.8-workbench-acceptance-20260724-r3`。
- 当前结论：**部分通过，仍不可发布。**

当前待验收候选包：

```text
E:\QRtest\QuickRec-v1.8-candidate-20260725-r11\QuickRec\QuickRec.exe
SHA256: C715897585C9EED4E164668EDA25AB11765D38F32D8A6C99979706637321BAD8
```

`r7` 已完成真实打包 GUI 自检，平均 `119.434 FPS`、最低每秒 `112 FPS`，并写入隔离能力缓存。随后优化检测进度表达：检测中固定使用常规数字百分比，通过、失败和取消使用矢量语义符号。真实 Windows 静态窗口已确认 `42%` 字形和对齐正常。`r9` 完成正式 120 FPS 无声、系统声音及麦克风/双音频阶段性取证；`r10` 修复工作台状态文案和设置弹窗中文按钮；`r11` 统一 Qt 标准按钮中文化后成为当前最终代码身份。

`r3` 完成工作台三类录制、设置、素材和诊断链路取证；`r4/r5` 只修复工作台入口与同进程页面记忆。D8/D9 后续新增了 120 FPS 正式链路，因此 `r5` 已失效，不能作为最终发布身份；未受修改影响的工作台证据可以继承。

## 2. 环境保护

1. 使用独立 `APPDATA`、配置、索引、日志和保存目录。
2. 录制和素材操作只使用验收目录中的受控文件。
3. 真实 `%APPDATA%\QuickRec\recordings.json` 未删除、覆盖或清空。
4. 验收结束后停止候选进程并保留全部证据。
5. QuickRec Lite 工作区保持干净。

真实索引核对值：

```text
C:\Users\win\AppData\Roaming\QuickRec\recordings.json
SHA256: B455034DC7F1755AA979DCF9B09A7563E129BEE4C2E246CD2D27D0279CCE167A
```

## 3. 候选身份与已有证据

| 项目 | 结论 | 证据 |
| --- | --- | --- |
| `r5` 候选包启动 | 通过 | 最终候选进程正常启动并驻留 |
| 包内 FFmpeg | 通过 | SHA256 `5AF82A0D4FE2B9EAE211B967332EA97EDFC51C6B328CA35B827E73EAC560DC0D` |
| 包内 FFprobe | 通过 | SHA256 `192A1D6899059765AC8C39764FC3148D4E6049955956DC2029F81F4BD6A8972D` |
| 静态桌面停止响应 | 通过 | BUG-V18-001 自动测试与硬件 smoke |
| 30 FPS 动态 smoke | 通过 | 285 帧，视频流正常 |
| 60 FPS 动态 smoke | 通过 | 566 帧，视频流正常 |
| 工作台视觉截图 | 通过 | `E:\QRtest\QuickRec-v1.8-ui-final` |
| 120 FPS 技术门禁 | 通过 | 专项优化后三次 1080p120 门禁全部通过 |
| 120 FPS 正式接入 | 部分通过 | 自检、快速校验、缓存失效、性能告警、无声和系统声音已闭合；仍待麦克风、双音频及 10 分钟音画同步 |

> 本节 `r5` 身份仅用于历史工作台证据。新候选包生成前，不存在可供 v1.8 最终发布的锁定 EXE。

## 4. 工作台基础链路

| 编号 | 操作与实际结果 | 结论 |
| --- | --- | --- |
| V18-W01 | 全新进程双击托盘图标，工作台打开并默认进入录制页 | 通过 |
| V18-W02 | 托盘“打开工作台”与双击共用同一入口；本轮未单独留存菜单点击截图 | 部分通过 |
| V18-W03 | 依次打开录制、素材库、设置和诊断，四页可用，无空白、乱码或异常窗口 | 通过 |
| V18-W04 | 在素材库页关闭工作台，再双击托盘打开；`r5` 恢复素材库页且未新建异常窗口 | 通过 |
| V18-W05 | 完全退出后启动 `r5`，首次打开默认进入录制页 | 通过 |
| V18-W06 | Windows Qt 集成验证将窗口放到屏幕外后重载，窗口被修正到 2560×1392 可见区域内 | 通过 |
| V18-W07 | 960×640 自动视觉证据无覆盖或溢出；最终候选真实最小尺寸未取证 | 部分通过 |

V18-W04 修复证据见 [bugfix-log.md](bugfix-log.md) 中 BUG-V18-002。

## 5. 工作台录制链路

| 编号 | 操作与实际结果 | 结论 |
| --- | --- | --- |
| V18-R01 | 从工作台启动全屏录制；工作台隐藏，保存后恢复并显示页内结果 | 通过 |
| V18-R02 | 手动选择区域并录制；输出为 1920×996@30 FPS，结果进入中央索引 | 通过 |
| V18-R03 | 选择“diagnostics - 文件资源管理器”并录制；输出画面对应目标窗口，未退化为全屏 | 通过 |
| V18-R04 | `r10` 录制中重新打开工作台，录制页显示只读状态；控制仍由浮动工具栏负责，侧栏同步显示“录制中” | 通过 |
| V18-R05 | `r11` 打包程序经全局快捷键完成三次全屏录制，浮动结果条实际显示“已保存 / 打开目录 / 素材库 / 关闭”；文件、日志和中央索引一致 | 通过 |
| V18-R06 | 区域选择器取消后工作台恢复；窗口选择取消未单独取证 | 部分通过 |

录制证据：

| 模式 | 文件 | FFprobe 结果 | 索引结果 |
| --- | --- | --- | --- |
| 全屏 | `QuickRec_20260724_222638.mp4` | H.264，1920×1080，30 FPS，83.766667 秒 | `fullscreen / none / available` |
| 区域 | `QuickRec_20260724_225109.mp4` | H.264，1920×996，30 FPS，26.166667 秒 | `region / none / available` |
| 窗口 | `QuickRec_20260724_223635.mp4` | H.264，1122×632，30 FPS，17.5 秒 | `window / none / available` |

完整文件均位于：

```text
E:\QRtest\QuickRec-v1.8-workbench-acceptance-20260724-r3\recordings
```

画面抽查：

```text
E:\QRtest\QuickRec-v1.8-workbench-acceptance-20260724-r3\region-recording-frame.png
E:\QRtest\QuickRec-v1.8-workbench-acceptance-20260724-r3\window-recording-frame.png
```

## 6. 设置、素材与诊断

| 编号 | 操作与实际结果 | 结论 |
| --- | --- | --- |
| V18-S01 | 修改倒计时设置后切页，出现“保存 / 放弃 / 取消”三项选择 | 通过 |
| V18-S02 | 选择取消，停留设置页并保留草稿 | 通过 |
| V18-S03 | 选择放弃，草稿恢复并完成切页 | 通过 |
| V18-S04 | Windows Qt 集成验证保存 FPS=60 后重载配置仍为 60；诊断目录保存后重载保持 | 通过 |
| V18-S05 | 受控注入临时文件写入失败，页面保留脏状态并显示失败阶段，正式配置未被覆盖 | 通过 |
| V18-M01 | 素材库显示本轮录制记录，详情与中央索引一致 | 通过 |
| V18-M02 | 搜索、录制模式和音频模式组合查询命中唯一受控素材；既有 GUI 状态保持证据继续有效 | 通过 |
| V18-M03 | 复制路径、中文空格路径重新定位、仅移除索引及受控副本移入 Windows 回收站均通过；视频与索引边界符合设计 | 通过 |
| V18-D01 | 复制诊断信息成功，剪贴板内容非空且可读 | 通过 |
| V18-D02 | 打开日志目录成功，资源管理器进入隔离诊断目录 | 通过 |
| V18-D03 | 导出文件成功，文件存在且可读 | 通过 |
| V18-D04 | 修改诊断目录后保存并重载，目录保持且页面脏状态清除 | 通过 |

诊断导出证据：

```text
E:\QRtest\QuickRec-v1.8-workbench-acceptance-20260724-r3\diagnostics\diagnostic_20260724_222353.txt
```

## 7. DPI

2026-07-25 使用 `r11` 同代码身份和真实 Windows Qt 平台完成进程级 DPI
复验。每档均在独立进程中设置 100%、125%、150% Qt 缩放，不修改 Windows
全局显示比例。候选 EXE 身份保持为：

```text
E:\QRtest\QuickRec-v1.8-candidate-20260725-r11\QuickRec\QuickRec.exe
SHA256: C715897585C9EED4E164668EDA25AB11765D38F32D8A6C99979706637321BAD8
```

本轮重新生成 69 张 Windows 原生 Qt 截图，覆盖四个工作台页面、录制成功和失败、
素材空状态和缺失状态、960×640 设置页、区域和窗口选择器、五类浮动工具栏状态、
120 FPS 检测运行/通过/失败状态、未保存设置、回收站确认框、120 快速校验和性能告警。

| 缩放 | 证据目录 | 结论 |
| --- | --- | --- |
| 100% | `E:\QRtest\QuickRec-v1.8-dpi-r11-20260725-windows\dpi-100` | 通过；四页、长路径、选择器、工具栏和确认框无裁切、重叠或不可点击控件 |
| 125% | `E:\QRtest\QuickRec-v1.8-dpi-r11-20260725-windows\dpi-125` | 通过；文字、按钮和状态完整，设置页滚动与固定底栏正常 |
| 150% | `E:\QRtest\QuickRec-v1.8-dpi-r11-20260725-windows\dpi-150` | 通过；960×640 逻辑尺寸下表单可滚动，保存底栏完整，关键弹窗完整 |

补充证据：

```text
E:\QRtest\QuickRec-v1.8-dpi-r11-20260725-windows\image-dimensions.csv
E:\QRtest\QuickRec-v1.8-dpi-r11-20260725-windows\sha256.txt
E:\QRtest\QuickRec-v1.8-dpi-r11-20260725-windows\contact-100.png
E:\QRtest\QuickRec-v1.8-dpi-r11-20260725-windows\contact-125.png
E:\QRtest\QuickRec-v1.8-dpi-r11-20260725-windows\contact-150.png
```

像素输出按比例增长：工作台主图从 1200×760 增长为 1500×950 和
1800×1140；960×640 最小窗口增长为 1200×800 和 1440×960。运行百分比
在 125% 下显示为正常 `42%` 数字；成功和失败继续使用矢量符号。未保存设置按钮
为“保存 / 放弃 / 取消”，回收站按钮为“是 / 否”。

证据边界：本轮属于真实 Windows Qt 渲染和交互控件状态复验，不改变系统全局 DPI；
如发布流程要求物理修改 Windows 显示缩放并重登，应另列为整机兼容性补充测试，
不作为当前 DPI 发布阻塞。

## 8. 120 FPS 边界

- D4 专项优化后三次 1080p120 技术门禁均通过。
- D8、D9 已恢复并完成正式能力服务、设置入口、快速校验、性能记录与结果提示。
- `r7` 打包 GUI 自检通过，D10.18 frozen 工具链验证完成。
- 当前候选为 `r11`。`r9/r10` 的自检与音频证据可继承；D11.23 已由缓存失效集成验证、快速校验分支测试和 Windows Qt 状态截图闭合。
- D11.19～D11.22 仍需补齐清晰麦克风内容、双音频 30 秒、双音频 10 分钟和音画同步测量。
- 未完成上述发布证据前，v1.8 仍为部分通过，不得进入发布收口。

## 9. 当前判断

工作台核心链路已经通过真实候选包验证：

- 四页路由、关闭隐藏、首次默认页和同进程页面记忆。
- 工作台来源全屏、区域和窗口录制。
- 设置未保存保护的保存、放弃、取消交互。
- 素材显示与搜索状态保持。
- 诊断复制、打开目录和导出。

仍待补证：

- 1080p120 麦克风清晰语音 30 秒。
- 1080p120 系统声音＋麦克风 30 秒。
- 1080p120 系统声音＋麦克风 10 分钟。
- 绝对音画偏移不超过 40 ms，10 分钟漂移增量不超过 20 ms。

工作台主链路、中文未保存提示、录制中只读状态与 120 FPS 技术门禁已经通过；v1.8 当前仍因麦克风清晰内容、双音频和音画同步证据未闭合而不可发布。

## 11. r9 音频取证与 r10 定向复验

| 项目 | 实际结果 | 结论 |
| --- | --- | --- |
| 1080p120 无声 | `r10` 输出 46.016667 秒 H.264，1920×1080@120，无音频流；平均 `119.925 FPS`，最低每秒 `116 FPS` | 通过 |
| 1080p120 系统声音 | `r9` 输出 49.725 秒，AAC 48kHz 双声道；测试音平均 `-37.1 dB`、峰值 `-32.3 dB` | 通过 |
| 1080p120 麦克风 | `r9` 输出 31.75 秒，麦克风链路、AAC 和入库正常；实际音量平均 `-69.9 dB`、峰值 `-49.1 dB` | 部分通过，需清晰语音补证 |
| 1080p120 双音频 | `r9` 日志确认系统声音与麦克风均初始化并生成输入文件，最终 AAC 正常；该轮没有持续系统测试音 | 部分通过 |
| 未保存提示 | `r10` 按钮为“保存 / 放弃 / 取消” | 通过 |
| 录制状态同步 | `r10` 录制页和侧栏均显示录制中，浮动工具栏仍为唯一控制入口 | 通过 |
| Qt 标准按钮中文化 | `r11` 素材库“仅移除索引”确认框显示“是 / 否”，取消后索引和视频均未改变 | 通过 |

`r10` 基础录制：

```text
E:\QRtest\QuickRec-v1.8-acceptance-20260725-r10\recordings\QuickRec_20260725_220534.mp4
```

`r10` 证据目录：

```text
E:\QRtest\QuickRec-v1.8-acceptance-20260725-r10\evidence
```

`r11` 中文标准按钮与基础录制证据：

```text
E:\QRtest\QuickRec-v1.8-acceptance-20260725-r11\evidence
E:\QRtest\QuickRec-v1.8-acceptance-20260725-r11\recordings\QuickRec_20260725_223226.mp4
```

## 10. 2026-07-25 打包 GUI 自检与终态显示

| 项目 | 实际结果 | 结论 |
| --- | --- | --- |
| `r7` 打包程序自检 | 平均 `119.434 FPS`，最低每秒 `112 FPS` | 通过 |
| 能力缓存 | `passed=true`，包含 300Hz 单显示器环境摘要和包内 FFmpeg 版本 | 通过 |
| 临时文件 | 自检完成后未在验收保存目录留下临时 MP4 | 通过 |
| 完成态显示 | 原 `100%` 改为绿色矢量对勾；失败为红色叉号，取消为灰色横线 | 自动视觉通过 |
| 运行百分比 | 真实 Windows 静态窗口显示标准 `42%`，居中且字形正常 | 通过 |
| `r9` 基础启动 | 进程正常启动并驻留 | 通过 |

证据：

```text
E:\QRtest\QuickRec-v1.8-acceptance-20260725-r7\gui-appdata\QuickRec\capture-capabilities.json
E:\QRtest\QuickRec-v1.8-acceptance-20260725-r7\evidence\selftest-complete-vector-preview.png
E:\QRtest\QuickRec-v1.8-acceptance-20260725-r8\evidence\selftest-progress-normal-windows-42.png
E:\QRtest\QuickRec-v1.8-acceptance-20260725-r9\evidence\selftest-progress-normal-windows-99.png
```

本项关闭 frozen 自检路径与完成态表达；快速校验已在后续自动边界补证中闭合，
但不替代麦克风、双音频和 10 分钟音画同步实测。

## 12. r11 自动边界与打包快捷键补证

Windows Qt 集成报告：

```text
E:\QRtest\QuickRec-v1.8-remaining-20260725-r11\automated-boundaries\verification-report.json
```

报告确认：

- 工作台离屏几何被修正到可见区域。
- 设置保存后持久化；受控写入失败时保留草稿并回滚正式配置。
- 诊断目录保存后重载保持。
- 素材组合查询、复制路径、中文空格路径重新定位、仅移除索引及受控副本移入回收站通过。
- 120 FPS 缓存仅在环境指纹一致时有效；刷新率、分辨率、保存磁盘、CPU、GPU、FFmpeg 和编码配置变化都会失效。

`r11` 打包程序隔离证据：

```text
E:\QRtest\QuickRec-v1.8-remaining-20260725-r11\recordings
E:\QRtest\QuickRec-v1.8-remaining-20260725-r11\appdata\QuickRec\recordings.json
E:\QRtest\QuickRec-v1.8-remaining-20260725-r11\diagnostics\quickrec.log
```

全局快捷键实际生成三条 1920×1080@30 FPS H.264 视频，均可解析并进入隔离中央索引。
停止后浮动结果条实际出现。Computer Use 无法点击另一个进程所属的悬浮层，因此按钮动作由
既有自动化测试覆盖；该限制不作为产品失败。
