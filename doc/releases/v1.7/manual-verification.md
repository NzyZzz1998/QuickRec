# QuickRec Full v1.7 GUI 手动验收清单

## 1. 验收对象

- 产品：QuickRec Full v1.7 候选包。
- 分支：`test`。
- EXE：`E:\QRtest\QuickRec-v1.7-candidate-fix1-dist\QuickRec\QuickRec.exe`。
- EXE SHA256：`8966985B6E1EBCEDC27499404BF8467133EFCC9C6A827898D8904CE137E15AF2`。
- 当前状态：技术验收通过；查询主链路、容量边界、待入库分区、异常降级、素材操作、三类录制、四类音频、诊断及真实系统 100%/125%/150% DPI 均已闭合，用户已授权进入正式发布收口。
- 正式发布版仍为 v1.6.1。

开始前重新计算 SHA256；若不一致，停止验收，不混用其他构建证据。

## 2. 隔离环境

1. 退出所有 QuickRec 进程。
2. 备份真实配置与 `%APPDATA%\QuickRec` 的索引文件。
3. 使用独立 APPDATA 和受控素材目录。
4. 准备 0/1/49/50/51/199/200 条正式素材夹具和待入库夹具。
5. 准备包含中文、空格、大小写拉丁字符、非法时间和空元数据的记录。
6. 验收结束后恢复 APPDATA、配置、DPI、权限与进程状态。

建议证据目录：`E:\QRtest\QuickRec-v1.7-acceptance`。

## 3. 查询验收

| 编号 | 操作 | 预期结果 | 状态 |
| --- | --- | --- | --- |
| V17-S1 | 输入文件名完整值 | 只显示匹配素材 | 通过 |
| V17-S2 | 输入文件名片段并混用大小写 | 拉丁字符不区分大小写 | 通过 |
| V17-S3 | 输入完整路径中的中文或目录片段 | 路径字段可匹配 | 通过 |
| V17-S4 | 同一素材文件名和路径同时匹配 | 结果只出现一次 | 待验证 |
| V17-S5 | 快速连续输入 | 约 150 ms 防抖后刷新，无明显卡顿 | 待验证 |
| V17-S6 | 点击搜索框清除图标 | 只清除关键词，筛选和排序不变 | 通过 |

2026-07-19 实测：文件名完整值、文件名片段、中文路径搜索、清除搜索和无结果状态均通过；搜索框 `Ctrl+A` 冲突已修复并在新候选包复验通过。快速连续输入和同一记录双字段匹配仍需补证。

## 4. 筛选与排序验收

| 编号 | 操作 | 预期结果 | 状态 |
| --- | --- | --- | --- |
| V17-F1 | 状态筛选各选项 | 可用、待入库、失败、缺失、信息不完整映射正确 | 待验证 |
| V17-F2 | 录制模式筛选 | 全屏、区域、窗口、未知正确 | 待验证 |
| V17-F3 | 音频模式筛选 | 无声、系统、麦克风、双音频、未知正确 | 待验证 |
| V17-F4 | 今天及最近 7/30/90 天 | 以本地时区和录制时间筛选 | 待验证 |
| V17-F5 | 搜索加多个筛选 | 不同类别使用 AND，无重复结果 | 通过 |
| V17-F6 | 重置条件 | 恢复空搜索、全部筛选和最新优先 | 通过 |
| V17-O1 | 时间最新/最早优先 | 顺序与受控索引一致 | 通过 |
| V17-O2 | 时长最长/最短优先 | 空值始终置后 | 通过 |
| V17-O3 | 文件最大/最小优先 | 空值始终置后 | 通过 |
| V17-O4 | 主值相同的多条记录 | 按录制时间倒序和稳定 ID 排序，不跳动 | 待验证 |

2026-07-19 实测：文件缺失、窗口录制、双音频筛选及三者 AND 组合通过；“今天”和重置条件通过；六种排序方向及空值置后通过。其余枚举逐项覆盖和稳定 ID 并列样本仍需补证。

## 5. 分区、分页与状态验收

| 编号 | 操作 | 预期结果 | 状态 |
| --- | --- | --- | --- |
| V17-P1 | 检查待入库和正式素材 | 两类保持独立分区、独立排序和计数 | 通过 |
| V17-P2 | 使用 51 条以上匹配素材 | 正式素材先显示 50 条，每次增加 50 条 | 通过 |
| V17-P3 | 检查待入库匹配项 | 待入库不分页 | 通过 |
| V17-P4 | 修改任一条件 | 正式素材重置到前 50 条 | 通过 |
| V17-P5 | 选中素材后刷新 | 仍匹配则保留选择，不匹配则清空且不跳选 | 通过 |
| V17-P6 | 依次验证 0/1/49/50/51/199/200 条 | 计数、按钮和空状态正确 | 通过 |
| V17-P7 | 条件下无匹配 | 显示明确无结果状态和重置入口 | 通过 |

2026-07-19 实测：60 条夹具默认显示 50 条，加载更多后显示 60 条；条件变化重置分页，选中项被过滤后详情清空，无匹配空状态正常。同进程关闭重开保留查询条件。0/1/49/50/51/199/200 独立边界均已补证通过。

## 6. 会话、异常与隐私

| 编号 | 操作 | 预期结果 | 状态 |
| --- | --- | --- | --- |
| V17-C1 | 设置条件后关闭并重开素材库 | 同进程恢复条件，分页回到 50，未恢复选中项 | 通过 |
| V17-C2 | 完全退出并重启应用 | 查询条件恢复默认 | 通过 |
| V17-C3 | 安全模拟查询异常 | 保留上一次结果并显示非阻断反馈 | 通过 |
| V17-C4 | 检查日志 | 不包含关键词、文件名、路径或具体条件 | 通过 |
| V17-C5 | 模拟索引加载失败 | 查询控件不可误操作，错误反馈清晰 | 通过 |

## 7. DPI 与回归

| 编号 | 操作 | 预期结果 | 状态 |
| --- | --- | --- | --- |
| V17-D1 | 100% DPI | 工具栏、列表、详情无关键裁切或重叠 | 通过 |
| V17-D2 | 125% DPI | 工具栏、列表、详情无关键裁切或重叠 | 通过 |
| V17-D3 | 150% DPI | 工具栏、列表、详情无关键裁切或重叠 | 通过 |
| V17-R1 | 打开、目录、复制、重定位、移除、回收站 | 操作对象正确，刷新保持查询条件 | 通过 |
| V17-R2 | 导入、重建、待入库重试 | 分区与计数正确，无功能回退 | 通过 |
| V17-R3 | 全屏、区域、窗口录制 | 视频正常保存并进入素材库 | 通过 |
| V17-R4 | 无声、系统声、麦克风、双音频 | 流与素材详情正确 | 通过 |
| V17-R5 | 设置与诊断 | 设置持久化、复制/打开/导出诊断正常 | 通过 |

2026-07-19 定向回归：新候选包已完成全屏、区域和窗口录制，三类文件均可由包内 FFprobe 解析并进入素材库。无声、系统声、麦克风和双音频均生成真实 MP4；系统声使用 880 Hz 受控信号，麦克风模式由独立设备初始化并产生非零音频。100%、125% 与 150% 均已在真实 Windows 缩放下通过。

## 8. 2026-07-19 补充验收证据

### 8.1 容量与分页

- 0、1、49、50、51、199、200 条均使用受控索引完成 GUI 验证。
- 50 条无“加载更多”；51 条先显示 50 条并出现按钮；200 条按 50 条递增至全部显示。
- 第 201 条真实录制入库后仍保留 200 条，最旧索引被淘汰，但对应测试视频未被删除。
- 从素材库仅移除测试索引后，总数由 200 变为 199，原视频仍存在。

### 8.2 录制与音频

| 模式 | 文件 | FFprobe / 索引证据 | 结论 |
| --- | --- | --- | --- |
| 全屏无声 | `E:\QRtest\QuickRec-v1.7-acceptance\recordings\QuickRec_20260719_201127.mp4` | H.264，1920×1080，30 FPS，无音频；索引 `fullscreen/none` | 通过 |
| 区域双音频 | `E:\QRtest\pkg_audio\QuickRec_20260719_211720.mp4` | H.264，1440×1080，30 FPS，AAC 48 kHz 双声道；索引 `region/both` | 通过 |
| 窗口双音频 | `E:\QRtest\pkg_audio\QuickRec_20260719_212217.mp4` | H.264，1122×632，30 FPS，AAC；抽帧为所选资源管理器窗口；索引 `window/both` | 通过 |
| 全屏系统声 | `E:\QRtest\pkg_audio\QuickRec_20260719_212407.mp4` | AAC 48 kHz 双声道；880 Hz 受控信号，平均 -31.3 dB、峰值 -21.0 dB；索引 `system` | 通过 |
| 全屏麦克风 | `E:\QRtest\pkg_audio\QuickRec_20260719_212546.mp4` | AAC 48 kHz 双声道；独立麦克风初始化，平均 -73.0 dB、峰值 -54.2 dB；索引 `microphone` | 通过（结合用户第一批人工补证） |

- 窗口录制抽帧：`E:\QRtest\QuickRec-v1.7-acceptance\evidence\v17-window-recording-frame.png`。
- 区域录制抽帧：`E:\QRtest\QuickRec-v1.7-acceptance\evidence\v17-region-recording-frame.png`。
- 录制文件哈希：`E:\QRtest\QuickRec-v1.7-acceptance\evidence\v17-recording-evidence-hashes.json`。
- 首次选择的资源管理器窗口在开录前失效，程序记录警告并取消，没有误生成全屏文件；随后选择稳定窗口复验通过。

### 8.3 DPI 与环境恢复

- 系统 `AppliedDPI=96`，100% 缩放下素材库工具栏、列表、详情及长路径显示正常。
- 125% 与 150% 均完成过独立 Qt 缩放补证，并进一步完成真实 Windows 缩放复验。
- 真实 Windows 125% 缩放已于 2026-07-19 补证：候选进程窗口 `GetDpiForWindow=120`，对应 125%。素材库主界面、长路径详情、重新定位文件对话框和移除索引确认框均无裁切、重叠或按钮越界；两个对话框均取消，未修改索引或文件。
- 125% 截图：`E:\QRtest\QuickRec-v1.7-acceptance\evidence\manual-final\V17-D2-125pct-material-library.jpg`、`V17-D2-125pct-relocate-dialog.jpg`、`V17-D2-125pct-remove-confirm.jpg`。
- 125% DPI、候选包和索引哈希证据：`E:\QRtest\QuickRec-v1.7-acceptance\evidence\manual-final\V17-D2-125pct-window-dpi.json`。
- 真实 Windows 150% 缩放已于 2026-07-19 补证：候选进程窗口 `GetDpiForWindow=144`，对应 150%。素材库主界面、长路径详情、重新定位文件对话框和移除索引确认框均无关键裁切、重叠或按钮越界；两个对话框均取消，未修改索引或文件。
- 150% 截图：`E:\QRtest\QuickRec-v1.7-acceptance\evidence\manual-final\V17-D3-150pct-material-library.jpg`、`V17-D3-150pct-material-details.jpg`、`V17-D3-150pct-relocate-dialog.jpg`、`V17-D3-150pct-remove-confirm.jpg`。
- 150% DPI、候选包和索引哈希证据：`E:\QRtest\QuickRec-v1.7-acceptance\evidence\manual-final\V17-D3-150pct-window-dpi.json`。
- 验收结束后停止全部 QuickRec 进程。
- 真实配置 SHA256 恢复为 `E3FC96863D862B4A2DB8080D1F3ECEC1565FF647B6A7D7D47DA6DE5038B4E8DD`。
- 真实索引 SHA256 恢复为 `B455034DC7F1755AA979DCF9B09A7563E129BEE4C2E246CD2D27D0279CCE167A`。
- 临时验收配置、索引和备份保存在 `E:\QRtest\QuickRec-v1.7-acceptance\restored-environment\round2-final`。

### 8.4 待入库启动恢复补证

- 使用独立 `APPDATA` 在 `E:\QRtest\QuickRec-v1.7-acceptance\pending-partition` 构造 1 条有效待入库记录。
- 锁定候选包启动后自动重试成功，待入库记录由 1 条变为 0 条，并进入正式中央索引；随后短录制也正常入库，正式索引共 2 条。
- 后续使用 55 条待入库与 8 条正式素材夹具完成 GUI 补证：两类独立分区、独立计数；待入库不分页，正式素材筛选不混入待入库记录。
- 重新定位并重试一条待入库记录后，待入库由 55 条降为 54 条，正式素材由 8 条增为 9 条，未产生重复记录。
- 该轮始终使用隔离 `APPDATA`，真实索引哈希保持不变。

### 8.5 异常、诊断与素材操作收口

- 查询异常定向测试通过：异常时保留上一次有效结果并显示非阻断反馈，日志不记录搜索词或素材路径；定向测试结果为 `2 passed`。
- 索引加载失败在候选包 GUI 中显示明确错误，查询控件不可误操作，待入库分区仍可见。
- 诊断复制、打开日志目录和导出均通过；导出文件为 `E:\QRtest\QuickRec-v1.7-acceptance\ops-final\diagnostics\diagnostic_20260720_001914.txt`，文件存在且内容可读。
- 受控素材 `素材操作 中文 空格.mp4` 的打开、打开目录、复制路径、仅移除索引和重新定位链路均通过。
- 2026-07-21 使用同一候选包重新执行回收站链路：确认框明确提示移入 Windows 回收站；用户确认后，素材库显示 0 条，原视频路径不存在，另一受控视频仍存在，中央索引由 1 条变为 0 条。
- 日志记录 `file moved to recycle bin`、`material recycled and index removed`，可区分实际文件回收与仅移除索引；未清空 Windows 回收站。
- 本轮曾因验收启动命令使用隐藏窗口参数导致素材库不可见；以正常可见方式重启后恢复，证实为验收环境问题，不是产品缺陷。

## 9. 剩余人工验证操作流程

### 9.1 开始前准备

1. 退出全部 QuickRec 进程。
2. 确认候选程序为 `E:\QRtest\QuickRec-v1.7-candidate-fix1-dist\QuickRec\QuickRec.exe`。
3. 在 PowerShell 执行：

```powershell
Get-FileHash E:\QRtest\QuickRec-v1.7-candidate-fix1-dist\QuickRec\QuickRec.exe -Algorithm SHA256
```

4. 结果必须为 `8966985B6E1EBCEDC27499404BF8467133EFCC9C6A827898D8904CE137E15AF2`；不一致时停止验收。
5. 备份 `%APPDATA%\QuickRec\config.json`、`recordings.json` 和 `pending-recordings.json`（如存在）。
6. 截图统一保存到 `E:\QRtest\QuickRec-v1.7-acceptance\evidence\manual-final`。

![[Pasted image 20260719220030.png]]

### 9.2 真实 Windows 125%/150% DPI

分别对 125% 和 150% 执行以下步骤：

1. 打开 Windows“设置 -> 系统 -> 屏幕 -> 缩放”，记录原缩放比例。
2. 将缩放设置为目标比例；若系统要求注销，应停止本项并记录“待验证”，不要强制注销当前工作环境。
3. 完全退出并重新启动候选程序，不能只关闭素材库窗口。
4. 从托盘打开“素材库”，依次检查搜索框、五个下拉框、重置按钮、计数、列表、详情和“加载更多”。
5. 选择一个包含中文和长路径的素材，确认路径换行后仍完整可读，按钮不被挤出窗口。
6. 分别打开重新定位、从素材库移除和移入回收站确认框，只检查布局后选择“取消”。
7. 打开设置页，确认素材入口与诊断分组无裁切。
8. 保存素材库主界面和至少一个确认框截图。
9. 两档验证结束后恢复原缩放比例，并重新检查一次桌面显示。

通过标准：文字无裁切，控件不重叠，按钮可点击，弹窗位于可视区域，点击位置与视觉位置一致。

### 9.3 待入库与正式素材分区

该项只能使用隔离环境，不能锁定或破坏真实用户索引。

1. 退出 QuickRec，在 `E:\QRtest\QuickRec-v1.7-acceptance\pending-partition` 准备独立 `APPDATA`。
2. 准备至少 2 条正式素材和 2 条待入库素材；待入库样本必须保持失败状态，不能在启动时自动恢复成功。
3. 可通过独占锁定隔离目录中的 `recordings.json` 制造中央索引写入失败；不得修改真实 `%APPDATA%` 权限。
4. 启动候选程序后，从托盘打开“素材库”。
5. 确认顶部显示“待入库 2 条”和“素材 2 条”，待入库位于独立分区且置顶。
6. 搜索一个只匹配待入库项的关键词，确认正式素材匹配数为 0，待入库结果仍展示且不出现“加载更多”。
7. 搜索一个只匹配正式素材的关键词，确认待入库匹配数为 0，正式素材正常分页。
8. 解除隔离索引锁，选择一条待入库记录执行重试；确认该记录从待入库分区消失，并且只在正式素材分区出现一次。
9. 关闭并重开素材库，确认分区、数量和结果保持一致。
10. 保存操作前、筛选后、重试后三张截图，并保存隔离 `recordings.json`、`pending-recordings.json` 和日志。

通过标准：两类记录不混排、计数独立、待入库不分页、重试成功不重复入库。若无法安全保持失败状态，标记“待验证”，不要使用真实索引试验。

### 9.4 素材操作回归

全部使用测试视频：

1. 在素材库选择有效素材，依次点击“打开”“打开目录”“复制路径”，确认对象均为当前选中素材。
2. 将一条测试视频移动到新目录，刷新后确认显示缺失，再通过“重新定位”选择新文件；重启后路径仍保持。
3. 对另一条测试素材选择“从素材库移除”：先取消一次，确认无变化；再次确认，确认索引消失但视频文件仍存在。
4. 对第三条测试素材选择“删除视频文件”：先取消一次；再次确认后检查文件进入 Windows 回收站，不能永久删除。
5. 执行“导入旧目录”和“重建目录”，确认查询条件不被重置、无重复记录、计数正确。
6. 每一步记录操作前后素材数量、文件状态和截图；不要清空回收站。

通过标准：所有操作都作用于选中对象，取消无副作用，重新定位可持久化，仅移除索引不删文件，删除操作进入回收站。

### 9.5 设置与诊断回归

1. 从托盘打开设置页，记录当前设置值。
2. 修改一个无风险设置并保存，完全退出后重启，确认设置保持；随后恢复原值。
3. 确认“诊断”分组存在。
4. 点击“复制诊断信息”，粘贴到临时文本文件，确认内容非空且可读。
5. 点击“打开日志目录”，确认资源管理器进入配置中的诊断目录。
6. 点击“导出诊断文件”，选择验收证据目录，确认导出文件存在且可读。
7. 完成一次短录制并重新打开素材库，确认诊断操作未影响录制、托盘和素材入口。
8. 保存设置页、日志目录和导出成功截图。

通过标准：设置持久化，三项诊断操作均成功，导出文件可读，不影响录制和素材库。

### 9.6 查询与索引异常

该项需要受控故障注入，不建议用户直接修改真实文件：

1. 使用独立 `APPDATA` 启动候选程序，并先让素材库成功显示一组受控结果。
2. 备份隔离索引后，使用测试工具令下一次查询抛出异常；触发条件变化，确认保留上一次有效列表并显示非阻断提示。
3. 恢复查询后再次改变条件，确认界面可继续使用。
4. 备份并替换隔离 `recordings.json` 为损坏 JSON，重新加载素材库。
5. 确认界面显示明确加载错误，查询控件不会对空对象执行破坏性操作，日志记录错误类别。
6. 恢复有效索引，确认素材库恢复；保存异常前、异常时和恢复后三组证据。

无法提供受控故障注入工具时，本项保持“待验证”；自动化测试通过不能替代该 GUI 结论。

### 9.7 验收后恢复

1. 停止所有 QuickRec 和故障注入辅助进程。
2. 恢复原 DPI、配置和真实索引。
3. 对比备份前后的配置、索引 SHA256。
4. 确认测试 ACL、文件锁和临时环境变量均已撤销。
5. 保留截图、日志、导出文件和隔离索引，不删除失败样本。

## 10. 通过标准

- 所有 P0 搜索、筛选、排序、分页与会话链路具有真实 GUI 证据。
- 三档 DPI 无关键控件不可用问题。
- 录制、音频、诊断和素材操作无发布阻塞回归。
- 自动化证据、GUI 证据和候选包 SHA256 指向同一构建。
- 任一发布阻塞缺陷未关闭时，结论不得写为通过。

完成后更新本文件、`verification.md` 和 `progress.md`；不要在进度看板中写排查流水。
