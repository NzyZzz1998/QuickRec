# QuickRec Full v1.9.4 原型自动验证记录

## 1. 验证对象

| 项 | 内容 |
| --- | --- |
| 验证日期 | 2026-07-29 |
| 原型入口 | `doc/releases/v1.9.4/prototype/index.html?page=exports` |
| 本地地址 | `http://127.0.0.1:8773/index.html?page=exports` |
| 全屏应用视图 | `http://127.0.0.1:8773/index.html?embed=1&page=exports` |
| 验证范围 | 导出配置、持久队列、任务状态、失败恢复、结果入库与剪辑工作台入口 |
| 产品确认 | 已完成（2026-07-29） |

本记录只证明高保真 HTML 原型的静态合同、浏览器交互和响应式布局检查通过，
不代表 PyQt 业务实现、FFmpeg 导出技术门禁或候选发布包验收通过。

## 2. 静态检查

| 检查项 | 结果 |
| --- | --- |
| `app.js` JavaScript 语法 | 通过 |
| `export.js` JavaScript 语法 | 通过 |
| `timeline.js` JavaScript 语法 | 通过 |
| DOM 重复 `id` | 0 |
| 控件实例 | 230 个 |
| 唯一交互合同 | 188 个 |
| 未注册交互合同 | 0 |
| 浏览器控制台和页面错误 | 0 |
| UTF-8 与乱码扫描 | 通过 |
| `git diff --check` | 通过 |

## 3. 浏览器与 DPI 检查

| 等效 DPI | 浏览器视口 | 文档宽度 | 结果 |
| --- | --- | --- | --- |
| 100% | 1200×760 | 1200 | 通过 |
| 125% | 960×640，设备缩放 1.25 | 960 | 通过 |
| 150% | 960×640，设备缩放 1.5 | 960 | 通过 |

三档检查均满足：

- 页面级横向溢出为 0；
- 导出任务列表、详情栏和底部操作区可达；
- 配置弹窗可滚动，底部操作不会遮挡字段；
- 关键按钮、状态标签和警告文案无明显裁切或重叠；
- 浏览器运行时错误为 0。

## 4. 导出状态矩阵

原型顶部“页面状态”共覆盖 24 个导出状态，自动切换全部通过：

| 分组 | 状态 |
| --- | --- |
| 空状态与预检 | 队列为空、预检通过、预检警告、预检失败 |
| 磁盘与文件冲突 | 空间提示、空间警告、空间不足、安全文件名、显式覆盖确认 |
| 任务运行 | 排队、校验、运行、疑似停滞、验证、原子提交 |
| 终态 | 成功、失败、取消、中断 |
| 结果入库 | 导出成功但入库失败 |
| 应用与队列恢复 | 退出确认、20 个未完成任务上限、队列损坏恢复、未知版本只读 |

状态切换失败数为 0，所有状态均保持“导出”一级页面可见。

## 5. 关键交互链路

| 交互 | 结果 |
| --- | --- |
| 剪辑工作台显示“导出项目”入口 | 通过 |
| “导出项目”打开三步配置弹窗 | 通过 |
| 输出规格进入预检 | 通过 |
| 预检通过后加入持久队列 | 通过 |
| 加入队列后切换到导出页 | 通过 |
| 失败任务重试进入校验阶段 | 通过 |
| 运行任务取消前二次确认 | 通过 |
| 取消后进入终态且保留诊断 | 通过 |
| 清理已完成历史前二次确认 | 通过 |
| 开发标注能定位“新建导出”合同 | 通过 |

## 6. 截图证据

截图位于本目录，主要证据包括：

- `timeline-export-entry-1440x900.png`
- `export-page-1440x900.png`
- `export-config-1440x900.png`
- `export-preflight-1440x900.png`
- `export-empty-1440x900.png`
- `export-preflight-warning-1440x900.png`
- `export-preflight-failed-1440x900.png`
- `export-overwrite-confirm-1440x900.png`
- `export-succeeded-1440x900.png`
- `export-ingest-failed-1440x900.png`
- `export-exit-prompt-1440x900.png`
- `export-queue-recovery-1440x900.png`
- `export-queue-unknown-1440x900.png`
- `export-page-dpi-100.png`
- `export-page-dpi-125.png`
- `export-page-dpi-150.png`

这些截图是设计门禁证据，不属于产品运行依赖，也不进入最终发布包。

## 7. 当前结论与剩余门禁

自动验证结论：**通过**。

产品负责人已确认：

1. 导出页双栏布局和信息密度；
2. 三步导出配置与预检节奏；
3. 默认安全文件名与显式覆盖二次确认；
4. “导出成功”和“素材入库失败”的结果分离；
5. 队列恢复、退出确认和未完成任务上限的表达。

FFmpeg 图、8 路音频和覆盖事务的开发前短样本技术门禁已经完成，证据见
`doc/releases/v1.9.4/export-technical-spike.md`。`dev_plan.md` 与
`progress.md` 也已输出，当前停在开发承接确认点，尚未获得业务实现授权。
